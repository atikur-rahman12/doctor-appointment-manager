import Login from "./Login";

export const metadata = {
  title: "DocAppoint | Login",
  description: ".......",
};

const LoginPage = () => {
  return (
    <div>
      <Login />
    </div>
  );
};

export default LoginPage;
